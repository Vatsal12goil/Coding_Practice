# Leetcode problem 
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dict ={}

        for i in range(len(nums)):
            need = target - nums[i]

            if need in dict:
                return [dict[need],i]
            dict[nums[i]] = i


Leetcode Q No. 164

class Solution:
    def maximumGap(self, nums: List[int]) -> int:
        
        nums.sort()

        max_gap = 0

        for i in range(1, len(nums)):
            gap = nums[i] - nums[i - 1]
            max_gap = max(max_gap, gap)

        return max_gap
